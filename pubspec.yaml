name: aplikasi_konsultasi
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  bloc: ^9.0.0
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  google_fonts: ^4.0.5
  carousel_slider: ^5.0.0
  shared_preferences: ^2.2.1
  intl: ^0.20.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
  - assets/logo/
  - assets/on_boarding/
  - assets/ilustration/
  - assets/icon/
